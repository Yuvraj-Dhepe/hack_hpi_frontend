# Install React Native CLI in your project or globally in your user space
npm install -g react-native-cli --prefix=$HOME/.npm-global

# Add to PATH (add to your .bashrc or .zshrc)
export PATH=$HOME/.npm-global/bin:$PATH